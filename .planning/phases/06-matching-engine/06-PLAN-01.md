---
wave: 1
depends_on: []
files_modified:
  - "src/lib/matching/matcher.ts"
autonomous: true
---

# Plan 1: The Heuristic Matching Engine

## Goal
Build the core business logic for identifying potential matches between a Missing person and a Found person.

<must_haves>
- A function `calculateMatchConfidence(missing: MockCase, found: MockCase): MatchResult`.
- Factors in Gender, Age, District, and basic keyword overlap.
</must_haves>

<task>
  <action>
    Create `src/lib/matching/matcher.ts`. 
    Define the `MatchResult` interface (score: number, reasons: string[]).
    Implement the `calculateMatchConfidence` function based on the heuristic rules defined in Research.
  </action>
  <read_first>
    - `src/lib/data/mockCases.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "calculateMatchConfidence" src/lib/matching/matcher.ts`
  </acceptance_criteria>
</task>

<task>
  <action>
    Export a helper `getHighConfidenceMatches(cases: MockCase[]): MatchPair[]` from `matcher.ts` that cross-references all 'missing' cases against all 'safe'/'injured' cases and returns pairs scoring >= 50.
  </action>
  <read_first>
    - `src/lib/matching/matcher.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "getHighConfidenceMatches" src/lib/matching/matcher.ts`
  </acceptance_criteria>
</task>
