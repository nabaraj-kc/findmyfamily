---
wave: 3
depends_on: ["03-PLAN-02.md"]
files_modified:
  - "src/lib/utils/offlineQueue.ts"
  - "src/app/actions/reportMissing.ts"
  - "src/app/[locale]/report-missing/page.tsx"
  - "src/app/[locale]/report-missing/success/page.tsx"
autonomous: true
---

# Plan 3: Offline Queueing & Server Action

## Goal
Implement robust submission, offline capability, and server-side handling.

<must_haves>
- Submissions are sent to a Next.js Server Action.
- Network failures gracefully fall back to `localStorage` offline queueing.
- User is redirected to a success page displaying their Case ID.
</must_haves>

<task>
  <action>
    Create `src/lib/utils/offlineQueue.ts`.
    Implement `savePendingSubmission(data)` and `getPendingSubmissions()`.
    Use `localStorage` with key `fmf_pending_submissions`.
  </action>
  <read_first>
    - `src/lib/utils/offlineQueue.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "fmf_pending_submissions" src/lib/utils/offlineQueue.ts`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/actions/reportMissing.ts` (Next.js Server Action).
    Export an async function `submitReportMissing(data: any)`.
    Generate a random Case ID in format `MP-2026-XXXX`.
    Return `{ success: true, caseId }`.
  </action>
  <read_first>
    - `src/app/actions/reportMissing.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "use server" src/app/actions/reportMissing.ts`
    - `grep -q "submitReportMissing" src/app/actions/reportMissing.ts`
  </acceptance_criteria>
</task>

<task>
  <action>
    Update `src/app/[locale]/report-missing/page.tsx` to handle submission.
    When `onComplete` is fired by `FormWizard`, attempt to call `submitReportMissing`.
    If network error, catch it, save using `savePendingSubmission`, and generate a local Case ID prefix like `PENDING-XXXX`.
    Redirect user to `/report-missing/success?caseId=XXX`.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/page.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "submitReportMissing" src/app/[locale]/report-missing/page.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/report-missing/success/page.tsx`.
    Display a success message, read `caseId` from searchParams, and render it using `CaseIdBadge`.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/success/page.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export default function SuccessPage" src/app/[locale]/report-missing/success/page.tsx`
  </acceptance_criteria>
</task>
