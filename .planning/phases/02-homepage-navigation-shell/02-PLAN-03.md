# Plan 3: Live Stats & Final Homepage Assembly

**Goal**: Build the live statistics section and finalize the homepage layout.

**Tasks**:
3.1 Create `src/components/organisms/StatsSection/StatsSection.tsx` and `StatsSection.module.css`.
  - Integrate the `StatsCounter` molecule.
  - Display mock data for "Active Cases", "Families Reunited", and "Total Reports".
  - Layout in a responsive grid (1 column mobile, 3 columns desktop).
3.2 Update `src/components/organisms/index.ts` to export Header, Footer, HeroSection, HomeSearch, and StatsSection.
3.3 Finalize `src/app/[locale]/page.tsx` by adding `<StatsSection>` below the search area.
3.4 Verify responsiveness, translation keys, and adherence to UI/UX Pro Max tokens across all new components.
