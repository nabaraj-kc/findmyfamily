---
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "next.config.ts"
  - "src/app/[locale]/layout.tsx"
autonomous: true
---

# Plan 1: PWA Configuration

## Goal
Transform the Next.js application into an installable Progressive Web App (PWA).

<must_haves>
- `@ducanh2912/next-pwa` installed and configured.
- `manifest.webmanifest` created in `public/`.
- App icon generated/placed in `public/icons/`.
- `themeColor` added to global layout metadata.
</must_haves>

<task>
  <action>
    Run `npm install @ducanh2912/next-pwa`. Update `next.config.ts` to wrap the config with `withPWA({ dest: 'public' })`.
  </action>
  <read_first>
    - `next.config.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "next-pwa" package.json`
    - `grep -q "withPWA" next.config.ts`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `public/manifest.webmanifest` with standard PWA fields.
  </action>
  <read_first>
    - `public/manifest.webmanifest` (if exists)
  </read_first>
  <acceptance_criteria>
    - file `public/manifest.webmanifest` exists and is valid JSON.
  </acceptance_criteria>
</task>
