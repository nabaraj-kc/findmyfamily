---
wave: 2
depends_on: ["04-PLAN-01.md"]
files_modified:
  - "src/app/[locale]/report-found/components/StatusStep.tsx"
  - "src/app/[locale]/report-found/components/BasicDetailsStep.tsx"
  - "src/app/[locale]/report-found/components/LocationStep.tsx"
  - "src/app/[locale]/report-found/components/FeaturesStep.tsx"
  - "src/app/[locale]/report-found/components/ReporterStep.tsx"
  - "src/app/[locale]/report-found/components/ReviewStep.tsx"
  - "src/app/[locale]/report-found/page.tsx"
autonomous: true
---

# Plan 2: Found Form Steps

## Goal
Create distinct form step components tailored for reporting found individuals.

<must_haves>
- StatusStep must present Safe, Injured, and Deceased options clearly.
- Copy must be sensitive and contextualized for finding someone.
</must_haves>

<task>
  <action>
    Create `StatusStep.tsx`. Present three large, selectable cards (using atoms) for Safe, Injured, and Deceased.
  </action>
  <read_first>
    - `src/components/atoms/Button/Button.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const StatusStep" src/app/[locale]/report-found/components/StatusStep.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `BasicDetailsStep.tsx`, `LocationStep.tsx`, `FeaturesStep.tsx`, `ReporterStep.tsx`, and `ReviewStep.tsx` within `report-found/components/`. Use `<FormField>` and atoms.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/components/BasicDetailsStep.tsx`
  </read_first>
  <acceptance_criteria>
    - `ls src/app/\[locale\]/report-found/components | grep ReviewStep.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Integrate the actual step components into the steps array in `report-found/page.tsx`.
  </action>
  <read_first>
    - `src/app/[locale]/report-found/page.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "StatusStep" src/app/[locale]/report-found/page.tsx`
  </acceptance_criteria>
</task>
