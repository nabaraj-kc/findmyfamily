---
wave: 1
depends_on: []
files_modified:
  - "src/lib/data/mockCases.ts"
  - "src/components/molecules/CaseCard/CaseCard.tsx"
  - "src/components/molecules/CaseCard/CaseCard.module.css"
  - "src/components/organisms/index.ts"
autonomous: true
---

# Plan 1: Mock Data & Case Card

## Goal
Build the foundational data mock and the UI component for rendering a single case.

<must_haves>
- Mock data includes a variety of statuses (missing, safe, injured).
- CaseCard molecule looks premium and handles missing images gracefully.
</must_haves>

<task>
  <action>
    Create `src/lib/data/mockCases.ts`. Export an array of ~10 mock cases using the `DATA_MODEL.md` schema properties. Export a `getCaseById` function.
  </action>
  <read_first>
    - `DATA_MODEL.md`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const mockCases" src/lib/data/mockCases.ts`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/components/molecules/CaseCard/CaseCard.tsx` and `.module.css`. It should accept a `caseData` prop. It should display the photo, name, age, last known location, and a status badge. It should link to `/cases/[caseId]`.
  </action>
  <read_first>
    - `src/components/atoms/Badge/Badge.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const CaseCard" src/components/molecules/CaseCard/CaseCard.tsx`
  </acceptance_criteria>
</task>
