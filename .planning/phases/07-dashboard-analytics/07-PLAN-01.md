---
wave: 1
depends_on: []
files_modified:
  - "src/lib/data/analytics.ts"
  - "src/components/molecules/KPICard/KPICard.tsx"
autonomous: true
---

# Plan 1: Analytics Utilities & KPI Molecule

## Goal
Establish the data calculation utilities and the visual component for displaying key metrics.

<must_haves>
- `analytics.ts` properly queries `mockCases.ts`.
- `KPICard` looks like a premium SaaS component.
</must_haves>

<task>
  <action>
    Create `src/lib/data/analytics.ts`. Import `mockCases`. Write and export functions: `getDashboardMetrics()` (returns counts for missing, found safe/injured, deceased/pending).
  </action>
  <read_first>
    - `src/lib/data/mockCases.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "getDashboardMetrics" src/lib/data/analytics.ts`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/components/molecules/KPICard/KPICard.tsx`. It should accept `title`, `value`, `icon` (name), and an optional `trend` (string or object with direction). Use `Icon` and basic styling.
  </action>
  <read_first>
    - `src/components/atoms/Icon/Icon.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const KPICard" src/components/molecules/KPICard/KPICard.tsx`
  </acceptance_criteria>
</task>
