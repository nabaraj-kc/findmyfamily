---
wave: 2
depends_on: ["05-PLAN-01.md"]
files_modified:
  - "src/app/[locale]/cases/page.tsx"
  - "src/app/[locale]/cases/components/CaseFilters.tsx"
autonomous: true
---

# Plan 2: The Search & Browse Directory

## Goal
Build the `/cases` directory page with filtering capability.

<must_haves>
- Uses URL search parameters for state (so links can be shared).
- Renders a grid of `CaseCard` components based on the filtered `mockCases`.
</must_haves>

<task>
  <action>
    Create `src/app/[locale]/cases/components/CaseFilters.tsx`. This should be a Client Component that reads `useSearchParams` and pushes updates to the router when filters (status, district, search query) change.
  </action>
  <read_first>
    - `src/components/atoms/Input/Input.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "useSearchParams" src/app/[locale]/cases/components/CaseFilters.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/cases/page.tsx`. This should be a Server Component that reads `searchParams`, filters the `mockCases` array accordingly, and renders `<CaseFilters>` alongside a grid of `<CaseCard>`s.
  </action>
  <read_first>
    - `src/app/[locale]/cases/components/CaseFilters.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "CaseCard" src/app/[locale]/cases/page.tsx`
  </acceptance_criteria>
</task>
