---
wave: 2
depends_on: ["08-PLAN-01.md"]
files_modified:
  - "src/components/layout/Header/Header.tsx"
  - "src/app/[locale]/layout.tsx"
autonomous: true
---

# Plan 2: Accessibility & Polish Audit

## Goal
Finalize the UI/UX, ensure a perfect Lighthouse accessibility score, and prepare the app for deployment.

<must_haves>
- High contrast colors.
- Aria-labels on icon-only buttons.
- Clean layout metadata.
</must_haves>

<task>
  <action>
    Audit `src/components/layout/Header/Header.tsx` and ensure the Language Toggle and Theme Toggle (if any) buttons have `aria-label`s. Update global layout metadata with standard PWA tags (apple-touch-icon, etc).
  </action>
  <read_first>
    - `src/app/[locale]/layout.tsx`
  </read_first>
  <acceptance_criteria>
    - Meta tags are correctly exported from layout.tsx.
  </acceptance_criteria>
</task>
