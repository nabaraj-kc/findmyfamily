---
wave: 2
depends_on: ["06-PLAN-01.md"]
files_modified:
  - "src/app/[locale]/admin/matches/components/MatchCard.tsx"
  - "src/app/[locale]/admin/matches/page.tsx"
autonomous: true
---

# Plan 2: Match Review UI

## Goal
Build the administrative dashboard where officials can review high-confidence algorithmic matches.

<must_haves>
- A side-by-side comparison UI (`MatchCard`).
- Visual highlighting of the confidence score.
- Actionable buttons (Confirm / Reject).
</must_haves>

<task>
  <action>
    Create `src/app/[locale]/admin/matches/components/MatchCard.tsx`. It takes a `MatchPair`. Display the missing person's basic info on the left, the found person's on the right. Show the `score` and `reasons` in the middle. Include "Confirm" and "Reject" buttons.
  </action>
  <read_first>
    - `src/components/atoms/Button/Button.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const MatchCard" src/app/[locale]/admin/matches/components/MatchCard.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/admin/matches/page.tsx`. A Client Component. Fetch matches using `getHighConfidenceMatches(mockCases)`. Render a list of `MatchCard`s. Handle the "Confirm" action by hiding the card and showing a success toast/message.
  </action>
  <read_first>
    - `src/lib/matching/matcher.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "getHighConfidenceMatches" src/app/[locale]/admin/matches/page.tsx`
    - `grep -q "MatchCard" src/app/[locale]/admin/matches/page.tsx`
  </acceptance_criteria>
</task>
