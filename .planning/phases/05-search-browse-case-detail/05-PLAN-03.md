---
wave: 3
depends_on: ["05-PLAN-01.md"]
files_modified:
  - "src/app/[locale]/cases/[caseId]/page.tsx"
  - "src/app/[locale]/cases/[caseId]/components/InformationModal.tsx"
autonomous: true
---

# Plan 3: Case Detail & "I Have Information"

## Goal
Build the dynamic case detail page and the critical tip-submission flow.

<must_haves>
- Shows full case details (timeline, features, reporter context).
- Includes a primary "I Have Information" button.
- That button opens a secure, masked modal for submitting tips.
</must_haves>

<task>
  <action>
    Create `src/app/[locale]/cases/[caseId]/components/InformationModal.tsx`. A client-side modal with a `<TextArea>` for the tip and an optional `<Input>` for contact info. Must simulate a secure submission.
  </action>
  <read_first>
    - `src/components/atoms/TextArea/TextArea.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const InformationModal" src/app/[locale]/cases/\[caseId\]/components/InformationModal.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/cases/[caseId]/page.tsx`. Fetch case by `caseId` from `mockCases`. Render the layout: Photo on left (or top), Details on right. Include the `InformationModal` triggered by a primary Button. If case is not found, return `notFound()`.
  </action>
  <read_first>
    - `src/lib/data/mockCases.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "notFound" src/app/[locale]/cases/\[caseId\]/page.tsx`
    - `grep -q "InformationModal" src/app/[locale]/cases/\[caseId\]/page.tsx`
  </acceptance_criteria>
</task>
