---
wave: 2
depends_on: ["07-PLAN-01.md"]
files_modified:
  - "src/app/[locale]/admin/layout.tsx"
  - "src/app/[locale]/admin/page.tsx"
autonomous: true
---

# Plan 2: Admin Dashboard Construction

## Goal
Build the `/admin` landing page combining the KPIs, recent activity, and navigation.

<must_haves>
- `layout.tsx` for `/admin` that provides basic admin navigation (Dashboard, Match Queue).
- `page.tsx` rendering a grid of `KPICard`s and a recent activity list.
</must_haves>

<task>
  <action>
    Create `src/app/[locale]/admin/layout.tsx`. Build a simple sub-navigation header specifically for the Admin section. Include links to `/admin` and `/admin/matches`.
  </action>
  <read_first>
    - `src/components/layout/Header/Header.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "AdminLayout" src/app/[locale]/admin/layout.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/admin/page.tsx`. Fetch `getDashboardMetrics()` and render 3-4 `KPICard`s in a grid. Below the grid, map over the last 5 cases in `mockCases` to render a simple "Recent Activity" table or list.
  </action>
  <read_first>
    - `src/lib/data/analytics.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "KPICard" src/app/[locale]/admin/page.tsx`
  </acceptance_criteria>
</task>
