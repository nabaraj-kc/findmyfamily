---
wave: 1
depends_on: []
files_modified:
  - "src/components/organisms/FormWizard/FormWizard.tsx"
  - "src/components/organisms/FormWizard/FormWizard.module.css"
  - "src/app/[locale]/report-missing/page.tsx"
autonomous: true
---

# Plan 1: Wizard Organism and Form State

## Goal
Build the multi-step wizard shell and the state management for the missing person form.

<must_haves>
- The FormWizard component handles 'currentStep' logic and 'Next/Back' navigation.
- The wizard renders ProgressDots correctly.
- The parent page `report-missing/page.tsx` lifts the form state and defines the 5 steps.
</must_haves>

<task>
  <action>
    Create `src/components/organisms/FormWizard/FormWizard.tsx` and its CSS module `FormWizard.module.css`.
    The FormWizard component must accept props: `steps` (array of objects with `title` and `component`), `onComplete` (callback), and `currentStep`/`setCurrentStep`.
    It should render the current step's component, along with a footer containing "Back" and "Next/Submit" buttons.
    It should display `ProgressDots` at the top.
  </action>
  <read_first>
    - `src/components/atoms/Button/Button.tsx`
    - `src/components/organisms/FormWizard/FormWizard.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const FormWizard" src/components/organisms/FormWizard/FormWizard.tsx`
    - `grep -q "onComplete" src/components/organisms/FormWizard/FormWizard.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/report-missing/page.tsx`.
    Define the state object `formData` matching the core fields of a missing person report (name, district, features, reporter name, etc.).
    Define a `steps` array with 5 placeholders for now.
    Render the `<FormWizard>` component passing the `steps` and state handlers.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/page.tsx`
    - `src/components/organisms/FormWizard/FormWizard.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "<FormWizard" src/app/[locale]/report-missing/page.tsx`
    - `grep -q "formData" src/app/[locale]/report-missing/page.tsx`
  </acceptance_criteria>
</task>
