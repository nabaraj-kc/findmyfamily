---
wave: 3
depends_on: ["04-PLAN-02.md"]
files_modified:
  - "src/app/actions/reportFound.ts"
  - "src/app/[locale]/report-found/success/page.tsx"
  - "src/app/[locale]/report-found/page.tsx"
autonomous: true
---

# Plan 3: Action, Moderation Routing & Success

## Goal
Wire up the Server Action to handle the submission, apply deceased-report logic, and show the success page.

<must_haves>
- `submitReportFound` action created.
- Deceased logic explicitly documented in the action.
- Offline queueing hooked up in `page.tsx`.
- Success page alters messaging if `status === 'deceased'`.
</must_haves>

<task>
  <action>
    Create `src/app/actions/reportFound.ts`. Mock database insert. If `data.status === 'deceased'`, log that `is_published` is forced to `false` for moderation. Generate `FP-2026-XXXX` case ID.
  </action>
  <read_first>
    - `src/app/actions/reportMissing.ts`
  </read_first>
  <acceptance_criteria>
    - `grep -q "submitReportFound" src/app/actions/reportFound.ts`
  </acceptance_criteria>
</task>

<task>
  <action>
    Update `report-found/page.tsx` `onComplete` handler to call the new Server Action, fallback to `savePendingSubmission`, and push to `/report-found/success` with query params (including `status`).
  </action>
  <read_first>
    - `src/app/[locale]/report-found/page.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "submitReportFound" src/app/[locale]/report-found/page.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/report-found/success/page.tsx`. Read `status` from searchParams. Display sensitive text if deceased. Re-use `CaseIdBadge`.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/success/page.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export default function SuccessPage" src/app/[locale]/report-found/success/page.tsx`
  </acceptance_criteria>
</task>
