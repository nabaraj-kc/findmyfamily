# Plan 2: Homepage Hero & Search Sections

**Goal**: Build the primary actions and visual entry point of the application.

**Tasks**:
2.1 Create `src/components/organisms/HeroSection/HeroSection.tsx` and `HeroSection.module.css`.
  - Implement a calm, premium visual design (gradient or optimized background).
  - Include `h1` Title and `p` Subtitle.
  - Include two prominent CTAs: "Report Missing" (Primary/Accent variant) and "Report Found" (Secondary variant).
2.2 Create `src/components/organisms/HomeSearch/HomeSearch.tsx` and `HomeSearch.module.css`.
  - Integrate the `SearchBar` molecule.
  - Add quick filter chips if applicable, or keep it simple with a centered search experience.
2.3 Update `src/app/[locale]/page.tsx` to render `<HeroSection>` and `<HomeSearch>`.
