---
wave: 2
depends_on: ["03-PLAN-01.md"]
files_modified:
  - "src/app/[locale]/report-missing/components/BasicDetailsStep.tsx"
  - "src/app/[locale]/report-missing/components/LocationStep.tsx"
  - "src/app/[locale]/report-missing/components/FeaturesStep.tsx"
  - "src/app/[locale]/report-missing/components/ReporterStep.tsx"
  - "src/app/[locale]/report-missing/components/ReviewStep.tsx"
  - "src/app/[locale]/report-missing/page.tsx"
autonomous: true
---

# Plan 2: Form Step Components

## Goal
Build the individual steps of the report missing flow.

<must_haves>
- All 5 steps must render as discrete components inside `report-missing/components/`.
- Forms must utilize the `FormField` and `Input` / `TextArea` atoms.
- Privacy consent must be included in the Reporter step.
</must_haves>

<task>
  <action>
    Create `src/app/[locale]/report-missing/components/BasicDetailsStep.tsx`.
    Include Inputs for: Full Name, Nickname, Age Range, Gender.
    Include a mock Photo Upload area (a box with a camera icon).
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/components/BasicDetailsStep.tsx`
    - `src/components/molecules/FormField/FormField.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const BasicDetailsStep" src/app/[locale]/report-missing/components/BasicDetailsStep.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/report-missing/components/LocationStep.tsx`.
    Include a `<select>` for District, and a `<TextArea>` for detailed location description.
    Include an Input (type=date) for Last Seen Date.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/components/LocationStep.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const LocationStep" src/app/[locale]/report-missing/components/LocationStep.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/report-missing/components/FeaturesStep.tsx`.
    Include `<TextArea>` for Distinguishing Features and Clothing.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/components/FeaturesStep.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const FeaturesStep" src/app/[locale]/report-missing/components/FeaturesStep.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/report-missing/components/ReporterStep.tsx`.
    Include Inputs for Reporter Full Name, Phone Number, and Relationship.
    Include a checkbox for Privacy Consent.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/components/ReporterStep.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const ReporterStep" src/app/[locale]/report-missing/components/ReporterStep.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `src/app/[locale]/report-missing/components/ReviewStep.tsx`.
    Render a read-only summary of the `formData` object.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/components/ReviewStep.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "export const ReviewStep" src/app/[locale]/report-missing/components/ReviewStep.tsx`
  </acceptance_criteria>
</task>

<task>
  <action>
    Integrate the new step components into the `steps` array in `src/app/[locale]/report-missing/page.tsx`, replacing the placeholders.
  </action>
  <read_first>
    - `src/app/[locale]/report-missing/page.tsx`
  </read_first>
  <acceptance_criteria>
    - `grep -q "BasicDetailsStep" src/app/[locale]/report-missing/page.tsx`
    - `grep -q "ReviewStep" src/app/[locale]/report-missing/page.tsx`
  </acceptance_criteria>
</task>
